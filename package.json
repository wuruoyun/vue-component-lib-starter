{
  "name": "vue-cli-template-lib",
  "version": "0.1.0",
  "license": "MIT",
  "description": "A template for Vue component library.",
  "scripts": {
    "docs": "cd docs && gitbook serve",
    "docs:deploy": "bash ./deploy-docs.sh"
  },
  "devDependencies": {
    "vue-cli": "^2.8.1"
  }
}
